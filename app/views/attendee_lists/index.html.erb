<p id="notice"><%= notice %></p>

<h1>Attendee Lists</h1>

<table>
  <thead>
    <tr>
      <th>Attendee List</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @attendee_lists.each do |attendee_list| %>
      <tr>
        <td><%= attendee_list.created_at %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
