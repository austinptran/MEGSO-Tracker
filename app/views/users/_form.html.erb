<%= form_with(model: users) do |form| %>
    <% if user.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(attendee_list.errors.count, "error") %> prohibited this user from being saved:</h2>
  
        <ul>
          <% attendee_list.errors.each do |error| %>
            <li><%= error.full_message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>
  
    <div class="field">
      <%= form.label :first_name, "First Name" %>
      <%= form.text_field :attendee_list_id %>
    </div>
  
    <div class="field">
      <%= form.label :UID, "UID" %>
      <%= form.text_field :UID %>
    </div>
  
    <div class="actions">
      <%= form.submit %>
    </div>
  <% end %>
  