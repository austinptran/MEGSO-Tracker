<%= form_with(model: point) do |form| %>
  <% if point.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(point.errors.count, "error") %> prohibited this point from being saved:</h2>

      <ul>
        <% point.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :point_count, "Point Count" %>
    <%= form.number_field :point_count %>
  </div>

  <div class="field">
    <%= form.label :reward, "Reward" %>
    <%= form.text_field :reward %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
